VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
# reCAPTCHA v3 site key for Firebase App Check (public; safe to ship). Leave blank to disable App Check.
VITE_FIREBASE_APPCHECK_SITE_KEY=
# Firebase Admin service account for the SF sync script (scripts/sync-sf.mjs).
# JSON string or path to the JSON file. Used by CI / local sync only — NOT the web app.
FIREBASE_SERVICE_ACCOUNT=
